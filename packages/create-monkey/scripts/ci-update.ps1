cd "template-empty"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-empty-ts"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-preact"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-preact-ts"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-react"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-react-ts"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-solid"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-solid-ts"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-svelte"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-svelte-ts"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-vanilla"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-vanilla-ts"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-vue"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..

cd "template-vue-ts"
pnpm --ignore-workspace install;pnpm --ignore-workspace up --latest; pnpm --ignore-workspace build
rm node_modules -Recurse;rm pnpm-lock.yaml;
cd ..
