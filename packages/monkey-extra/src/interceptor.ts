// import axios from 'axios';

// window.fetch;
// axios.interceptors.request.use(
//   (config) => {
//     return config;
//   },
//   (error) => {},
// );

// export type InterceptorManager = {
//   use(
//     onFulfilled?: (value: Request) => Request | Promise<Request>,
//     onRejected?: (error: any) => any,
//     options?: unknown,
//   ): number;
//   eject(id: number): void;
//   clear(): void;
// };

// const request = {
//   use: (config: Request): number => {
//     return 0;
//   },
// };
